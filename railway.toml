[build]
builder = "NIXPACKS"
buildCommand = "npm install --include=dev"

[deploy]
startCommand = "node node_modules/.bin/tsx apps/api/src/main.ts"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
healthcheckPath = "/health"
healthcheckTimeout = 300
