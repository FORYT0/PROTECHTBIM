[build]
builder = "NIXPACKS"
buildCommand = "npm install && npx nx reset && npm run build"

[deploy]
startCommand = "node dist/apps/api/src/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 120
