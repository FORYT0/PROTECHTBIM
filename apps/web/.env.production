# Production environment variables for Vercel deployment
# These VITE_ vars are baked into the JS bundle at build time

# API backend hosted on Render
VITE_API_URL=https://protecht-bim-api.onrender.com/api/v1
VITE_WS_URL=https://protecht-bim-api.onrender.com
