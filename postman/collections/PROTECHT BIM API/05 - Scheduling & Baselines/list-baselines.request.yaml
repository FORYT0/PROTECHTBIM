$kind: http-request
name: 'List Baselines'
method: GET
url: '{{baseUrl}}/api/v1/projects/:id/baselines'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '{{projectId}}'
