$kind: http-request
name: 'Get Baseline Variance'
method: GET
url: '{{baseUrl}}/api/v1/baselines/:id/variance'
order: 6000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '{{baselineId}}'
