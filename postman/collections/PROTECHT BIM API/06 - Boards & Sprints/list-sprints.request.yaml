$kind: http-request
name: 'List Sprints'
method: GET
url: '{{baseUrl}}/api/v1/projects/:id/sprints'
order: 4000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '{{projectId}}'
