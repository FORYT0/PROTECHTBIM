$kind: http-request
name: 'List Boards'
method: GET
url: '{{baseUrl}}/api/v1/projects/:id/boards'
order: 1000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '{{projectId}}'
