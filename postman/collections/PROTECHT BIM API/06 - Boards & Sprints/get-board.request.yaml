$kind: http-request
name: 'Get Board'
method: GET
url: '{{baseUrl}}/api/v1/boards/:id'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '{{boardId}}'
