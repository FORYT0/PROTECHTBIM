$kind: http-request
name: 'Get Project'
method: GET
url: '{{baseUrl}}/api/v1/projects/:id'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '{{projectId}}'
