$kind: http-request
name: 'Delete Project'
method: DELETE
url: '{{baseUrl}}/api/v1/projects/:id'
order: 5000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '{{projectId}}'
