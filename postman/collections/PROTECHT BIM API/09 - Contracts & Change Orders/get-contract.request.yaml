$kind: http-request
name: 'Get Contract'
method: GET
url: '{{baseUrl}}/api/v1/contracts/:id'
order: 4000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '{{contractId}}'
