$kind: http-request
name: 'Get Time Entry'
method: GET
url: '{{baseUrl}}/api/v1/time_entries/:id'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '{{timeEntryId}}'
