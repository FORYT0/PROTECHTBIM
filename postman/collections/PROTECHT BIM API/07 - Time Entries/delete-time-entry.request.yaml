$kind: http-request
name: 'Delete Time Entry'
method: DELETE
url: '{{baseUrl}}/api/v1/time_entries/:id'
order: 5000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '{{timeEntryId}}'
