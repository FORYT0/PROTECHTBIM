$kind: http-request
name: 'Get Work Package'
method: GET
url: '{{baseUrl}}/api/v1/work_packages/:id'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: id
    value: '{{workPackageId}}'
