$kind: http-request
name: 'Get BIM Model'
method: GET
url: '{{baseUrl}}/api/v1/bim/models/:modelId'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: modelId
    value: '{{modelId}}'
