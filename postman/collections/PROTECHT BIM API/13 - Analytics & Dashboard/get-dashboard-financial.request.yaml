$kind: http-request
name: 'Get Dashboard Financial'
method: GET
url: '{{baseUrl}}/api/v1/projects/:projectId/dashboard/financial'
order: 2000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: projectId
    value: '{{projectId}}'
