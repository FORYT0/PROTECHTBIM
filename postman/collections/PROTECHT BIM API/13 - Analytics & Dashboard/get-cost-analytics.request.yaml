$kind: http-request
name: 'Get Cost Analytics'
method: GET
url: '{{baseUrl}}/api/v1/projects/:projectId/analytics/cost'
order: 6000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: projectId
    value: '{{projectId}}'
