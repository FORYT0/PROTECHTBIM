$kind: http-request
name: 'Get Dashboard Analytics'
method: GET
url: '{{baseUrl}}/api/v1/projects/:projectId/dashboard/analytics'
order: 3000
headers:
  - key: Authorization
    value: 'Bearer {{authToken}}'
pathVariables:
  - key: projectId
    value: '{{projectId}}'
